python -m virtualenv venv 
.\venv\Scripts\activate - utilice esto para activar el entorno virtual

pip freeze
pip install boto3 
pip install dotenv
pip install bcrypt
pip install request
pip install sys

use esos comandos para instalar las librerías necesarias así no tendrá problemas al ejecutar el programa.